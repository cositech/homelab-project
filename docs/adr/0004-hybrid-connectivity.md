# ADR 0004 — Hybrid direct and gateway connectivity

**Status:** Accepted

## Decision

Direct mode remains for homelabs. Gateway mode becomes the preferred enterprise/MSP
model. Both consume equivalent normalized contracts.

## Consequences

- implementation must follow this decision unless superseded by a later ADR;
- deviations require an explicit ADR or documented compatibility exception.

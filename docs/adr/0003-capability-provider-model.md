# ADR 0003 — Capability-based provider architecture

**Status:** Accepted

## Decision

Provider adapters expose small normalized capabilities instead of adding one-off
top-level application architectures for every service.

## Consequences

- implementation must follow this decision unless superseded by a later ADR;
- deviations require an explicit ADR or documented compatibility exception.
